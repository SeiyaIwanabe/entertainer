.RegistraionFeild
  .RegistraionFeild__Form
    %h2 アカウントを作成
    = form_with model: @user, url: user_registration_path, id: 'new_user', class: 'new_user', local: true do |f|
      = devise_error_messages!
      .field
        = f.label :お名前
        %em (最大10文字)
        %br/
        = f.text_field :nickname, autofocus: true, maxlength: "10", class: "registration-name"
      .field
        = f.label :メールアドレス
        %br/
        = f.email_field :email, class: "registration-email"
      .field
        = f.label :パスワード
        - if @validatable
          %em
            (#{@minimum_password_length} characters minimum)
        %br/
        = f.password_field :password, autocomplete: "off", class: "registration-password"
      .field
        = f.label :password_confirmation
        %br/
        = f.password_field :password_confirmation, autocomplete: "off", class: "registration-confirmation"
      .actions
        = f.submit "登録する", class: "registration-btn"

