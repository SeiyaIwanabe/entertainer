.EventField
  .search_wap
   = search_form_for @search do |f|
     = f.text_field :prefecture_cont , placeholder: "何かお探しですか？", class: 'input'
       = button_tag type: 'submit', class: 'search__button' do
         .icon_wap
           %i.fas.fa-search
  .EventField__outline
    - @events.each do |event|
      = link_to(event_path(event.id), class: "event-show-btn") do
        .EventInfo
          .EventInfo__prefecture
            = event.prefecture
          .EventInfo__main
            %p.event-name
              = event.event_name
            .EventHost--Area
              %p.host
                主催者：
              %p.host-name
                = event.recruiter.nickname
            %p.enter-judge
              申し込み可能です！
    .pagination-area
      = paginate @events